this is a simple repository where i try to learn the foundations for the Web Develpoment folllowing The odin Project

GIT CHEATSHEET:

git clone git@github.com:userName/repoName.git
git push (if there is only one branch)
git push origin main(if multiBranch)

git add .
git add <fileName>
git commit -m"messaage"

git status
git log

The basic of Git syntax is program|action|destination
